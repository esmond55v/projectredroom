{
  "name": "projectredroom",
  "version": "0.1.0",
  "private": true,
  "description": "PROJECT REDROOM — official site",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
