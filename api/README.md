# Vercel Serverless Functions

This directory will hold serverless functions when Stripe is added.

Planned files:
- `create-checkout.js` — creates a Stripe Checkout session for the buy flow
- `webhook.js` — receives Stripe webhook events on payment completion

Currently empty. Vercel auto-detects any `.js` file added here as a serverless function.
